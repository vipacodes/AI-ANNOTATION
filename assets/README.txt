These four SVGs are illustrative reconstructions of the shape of a real contributor workspace.
They are not screenshots of any vendor product and contain no logos, brand marks or copied UI assets.
Use freely; label them as illustrative if you republish them.

PNGs are optimised (-strip, q82). Regenerate with:
  node -e "const{Mockups}=0"  # or: convert assets/mockup-squad.svg -resize 1320x800 -background '#0e131c' -flatten -strip -quality 82 assets/mockup-squad.png
